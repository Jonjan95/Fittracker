services:
  db:
    image: mysql:8
    container_name: fittracker-db
    restart: unless-stopped
    ports:
      - "3308:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: fittracker-db
    volumes:
      - fittracker-data:/var/lib/mysql

volumes:
  fittracker-data:
