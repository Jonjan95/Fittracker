# Fittracker — frontend

Next.js + React UI for the Fittracker app. See the [project root README](../README.md) for the full overview, architecture, and setup instructions.

## Quick start (assumes the backend and database are running)

```bash
pnpm install
pnpm dev
```

Then open `http://localhost:3000`.
